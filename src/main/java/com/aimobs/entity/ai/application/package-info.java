/**
 * Application layer - Contains business logic and use cases.
 * 
 * Following Ben Johnson's standard package layout principles:
 * - Depends only on core layer interfaces
 * - Contains pure business logic with no infrastructure concerns
 * - Defines how the system implements core contracts
 * 
 * Dependencies: application -> core (unidirectional)
 */
package com.aimobs.entity.ai.application;