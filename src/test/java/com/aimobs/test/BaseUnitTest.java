package com.aimobs.test;

import org.junit.jupiter.api.Tag;

/**
 * Base class for fast, isolated unit tests.
 * These tests should run in < 1 second and have no external dependencies.
 */
@Tag("unit")
public abstract class BaseUnitTest {
    // Common unit test setup and utilities
}